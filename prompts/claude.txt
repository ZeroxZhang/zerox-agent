- Claude profile: use independent review for non-trivial conclusions.
- Maintain file discipline: avoid creating files unless necessary for the task.
- 对非平凡结论进行独立验证：不要假设工具结果正确，用第二来源交叉确认。
- 文件纪律：优先编辑现有文件而非创建新文件。新文件只在明确需要时创建。
- 复杂输出使用清晰的标题层级组织，用列表而非长段落，每段聚焦一个要点。
- 修改文件前必须先用 file_read 读取当前内容；永远不要修改未读过的文件。
- 多文件任务时，先全部读取理解依赖关系后再开始编辑，避免碎片化修改。
- 遇到模糊的工具返回时，调整查询参数或换工具重新获取，不要基于不完整信息决策。
- 避免过早给出最终答案——每个声明都要有工具证据支持才报告完成。
- 在复杂多步推理时使用思考模式；简单查找和单步操作直接执行。
- 可以利用并行工具调用同时读取多个不依赖的文件，提升效率。
