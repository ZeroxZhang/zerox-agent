# v2.9.0 Output Rendering Task 3 Report

Date: 2026-06-26
Branch: `codex/2.9.0`

## Scope

Implemented Task 3: Markdown, Table, Code, And Diff Model.

Changed files:
- `src/renderer/chatMarkdown.ts`
- `src/renderer/chatMarkdown.test.ts`
- `src/renderer/chatOutputModel.ts`
- `src/renderer/chatOutputModel.test.ts`
- `src/renderer/components/AgentChatPanel.tsx`

## RED Evidence

Command:

```bash
npm test -- src/renderer/chatMarkdown.test.ts src/renderer/chatOutputModel.test.ts
```

Result: failed as expected before implementation.

Key output:

```text
FAIL src/renderer/chatOutputModel.test.ts
Error: Cannot find module './chatOutputModel'

FAIL src/renderer/chatMarkdown.test.ts > parses markdown tables into table blocks
expected paragraph to equal table block

FAIL src/renderer/chatMarkdown.test.ts > parses blockquotes into blockquote blocks without raw HTML
expected paragraph to equal blockquote block

FAIL src/renderer/chatMarkdown.test.ts > parses task list items with checked metadata
expected unorderedList to equal taskList block

Test Files 2 failed (2)
Tests 3 failed | 4 passed (7)
```

The diff language metadata test was added during RED and already passed against existing fenced-code behavior.

## GREEN Evidence

Command:

```bash
npm test -- src/renderer/chatMarkdown.test.ts src/renderer/chatOutputModel.test.ts
```

Result:

```text
Test Files 2 passed (2)
Tests 11 passed (11)
```

After a small parser guard refactor, the same focused test command was re-run and stayed green:

```text
Test Files 2 passed (2)
Tests 11 passed (11)
```

## Broader Verification

Command:

```bash
npm test -- src/renderer/chatMarkdown.test.ts src/renderer/chatOutputModel.test.ts src/renderer/chatStreamReducer.test.ts
```

Result:

```text
Test Files 3 passed (3)
Tests 18 passed (18)
```

Command:

```bash
npm run build
```

Result: passed. Vite emitted the existing large-chunk warning.

Command:

```bash
npm run harness:check
```

Result:

```text
Harness check passed.
```

Command:

```bash
git diff --check
```

Result: passed with no output.

Additional repo guide checks:

```bash
npm run verify
```

Result: still fails in `src/shared/packageScripts.test.ts` because the repo-level v2.8.5 release gate expects no unfinished features while `P23-v2.9.0-output-rendering` is intentionally still `planned`.

```text
Test Files 1 failed | 178 passed (179)
Tests 1 failed | 1222 passed (1223)
FAIL src/shared/packageScripts.test.ts > package scripts > keeps release gates done through v2.8.5
Received: P23-v2.9.0-output-rendering status planned
```

```bash
npm run smoke:prod
```

Result: passed. The local `better-sqlite3` native module ABI mismatch triggered the existing JSON storage fallback, then smoke completed:

```text
Smoke startup passed: renderer rendered agent chat UI.
```

Additional repo guide checks:

```bash
npm run verify
```

Result: failed in `src/shared/packageScripts.test.ts` because the repo-level v2.8.5 release gate expects no unfinished features while `P23-v2.9.0-output-rendering` is intentionally still `planned`.

```text
FAIL src/shared/packageScripts.test.ts > package scripts > keeps release gates done through v2.8.5
expected featureList.features.filter((feature) => feature.status !== "done") to deeply equal []
Received: P23-v2.9.0-output-rendering status planned
```

```bash
npm run smoke:prod
```

Result: passed. The local `better-sqlite3` native module ABI mismatch triggered the existing JSON storage fallback, then smoke completed:

```text
Smoke startup passed: renderer rendered agent chat UI.
```

## Implementation Notes

- Extended `parseMarkdownBlocks` with table, blockquote, and task-list blocks while preserving existing heading, paragraph, unordered list, ordered list, code, and inline markdown tests.
- Added `parseChatMarkdown` and `ChatMarkdownBlock` compatibility exports.
- Added `chatOutputModel` normalization for persisted `ChatMessageRecord.outputParts`, legacy markdown fallback text parts, and live `output_part` stream events with stable `renderKey` and `source`.
- Updated the existing chat panel to handle the expanded markdown block union with simple semantic fallback rendering only; final visual treatment remains for later renderer tasks.

## Review Blocker Follow-up

Addressed follow-up review blockers from commit `512be60`:

- `AgentChatPanel` now renders assistant message bodies through the output model helper, so persisted/live `outputParts` are visible through the existing markdown pipeline.
- `outputPartsFromMessage()` preserves legacy `message.content` as fallback when non-empty `outputParts` do not contain a text part.
- `outputPartFromStreamEvent()` now uses `requestId + part.id` for render identity, matching reducer upsert identity instead of stream sequence.

### Follow-up RED Evidence

Command:

```bash
npm test -- src/renderer/chatOutputModel.test.ts src/renderer/chatMarkdown.test.ts
```

Result: failed as expected before implementation.

Key output:

```text
Test Files 1 failed | 1 passed (2)
Tests 4 failed | 10 passed (14)

FAIL preserves legacy content when persisted output parts do not include text
expected outputPartsFromMessage(message) to include legacy text fallback

FAIL builds markdown for assistant messages from text and evidence parts
TypeError: outputMarkdownFromMessage is not a function

FAIL converts live output_part stream events into renderable parts
expected renderKey "r1:diff_1", received "r1:7:diff_1"

FAIL keeps the same render key for repeated live updates to the same output part
expected stable renderKey across sequence 7 and sequence 8
```

### Follow-up GREEN Evidence

Command:

```bash
npm test -- src/renderer/chatOutputModel.test.ts src/renderer/chatMarkdown.test.ts
```

Result:

```text
Test Files 2 passed (2)
Tests 14 passed (14)
```

After a test wording cleanup, the same focused command was re-run and stayed green:

```text
Test Files 2 passed (2)
Tests 14 passed (14)
```

### Follow-up Verification

Command:

```bash
npm test -- src/renderer/chatMarkdown.test.ts src/renderer/chatOutputModel.test.ts src/renderer/chatStreamReducer.test.ts
```

Result:

```text
Test Files 3 passed (3)
Tests 21 passed (21)
```

Command:

```bash
npm run build
```

Result: passed. Vite emitted the existing large-chunk warning.

Command:

```bash
npm run harness:check
```

Result:

```text
Harness check passed.
```

Command:

```bash
git diff --check
```

Result: passed with no output.
