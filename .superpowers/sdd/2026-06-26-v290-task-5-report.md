# v2.9.0 Task 5 Report - Approved Visual Styling And Responsive Safety

## Request

Implement Task 5 for v2.9.0 output rendering: translate the approved Evidence-Linked Answer plus Run Ledger direction into repo-local CSS, stable renderer class hooks, responsive-safe structured output styling, and a committed design artifact.

## Changed Files

- `src/renderer/materialDesign.test.ts`
- `src/renderer/components/chat/AnswerBlock.tsx`
- `src/renderer/components/chat/CodeBlockView.tsx`
- `src/renderer/components/chat/OutputPartRenderer.tsx`
- `src/renderer/components/chat/EvidenceRail.tsx`
- `src/renderer/components/chat/RunLedgerView.tsx`
- `src/renderer/styles/chat.css`
- `src/renderer/styles/responsive.css`
- `docs/design/zerox-agent-2-9-0-output-rendering-artifact.html`
- `.superpowers/sdd/2026-06-26-v290-task-5-report.md`
- `.zerox/progress.md`

## RED Evidence

- `npm test -- src/renderer/materialDesign.test.ts -t "v2.9 output rendering"` failed as expected before implementation.
- Failure 1: `covers v2.9 output rendering CSS hooks for approved visual styling` did not find `.chat-answer-block` in the stylesheet bundle.
- Failure 2: `commits the v2.9 output rendering design artifact with acceptance states` did not find `docs/design/zerox-agent-2-9-0-output-rendering-artifact.html`.

## GREEN Evidence

- `npm test -- src/renderer/materialDesign.test.ts -t "v2.9 output rendering"` -> 1 file passed, 2 tests passed, 51 skipped.
- `npm test -- src/renderer/materialDesign.test.ts src/renderer/chatOutputModel.test.ts src/renderer/chatMarkdown.test.ts src/renderer/chatStreamReducer.test.ts src/shared/chatOutput.test.ts` -> 5 files passed, 81 tests passed.

## Verification Evidence

- `npm run build` -> passed, with the existing Vite chunk-size warning.
- `npm run harness:check` -> passed.
- `npm run smoke:prod` -> passed; renderer rendered agent chat UI. Existing better-sqlite3 ABI mismatch fell back to JSON.
- `npm run verify` -> failed in pre-existing release-gate coverage: `src/shared/packageScripts.test.ts` expects no unfinished features through v2.8.5, but `.zerox/feature_list.json` still has `P23-v2.9.0-output-rendering` marked `planned`.
- `git diff --check` -> passed.

## Browser QA Evidence

- Static artifact at `http://127.0.0.1:4177/docs/design/zerox-agent-2-9-0-output-rendering-artifact.html` desktop 1280x900 -> title matched, console logs empty, 10 acceptance states rendered, 3 answer blocks present, no page-level horizontal overflow, no overflowing cards or blocks, screenshot captured.
- Static artifact mobile 390x844 -> console logs empty, answer blocks stacked to one column, no page-level horizontal overflow, no overflowing cards or blocks; the data table used one expected internal horizontal scroll container.
- Renderer dev app at `http://127.0.0.1:5173/#chat` desktop 1280x900 -> no Vite overlay, console logs empty, chat panel and composer present, and clicking a prompt suggestion populated the focused textarea.

## Review Blocker Fix Evidence

- Fixed the review blocker where body-only assistant answers still reserved an empty evidence column. `AnswerBlock` now adds `has-evidence` only when the evidence rail is rendered and uses `is-body-only` otherwise.
- Fixed the review blocker where title-only ledger events were compressed by absent optional columns. `RunLedgerView` now emits `has-detail`, `has-tool`, and `is-title-only`, and CSS switches ledger columns by those states.
- Added mobile-specific `.chat-answer-block.has-evidence` CSS so the higher-specificity desktop evidence rule does not override the 640px single-column breakpoint.
- RED: `npm test -- src/renderer/materialDesign.test.ts -t "avoids empty structured output columns"` failed before the fix because the state classes were missing.
- RED: `npm test -- src/renderer/materialDesign.test.ts -t "covers v2.9 output rendering CSS hooks"` failed after the first fix until the mobile `.has-evidence` override was added.
- GREEN: `npm test -- src/renderer/materialDesign.test.ts -t "avoids empty structured output columns"` -> selected test passed.
- GREEN: `npm test -- src/renderer/materialDesign.test.ts -t "covers v2.9 output rendering CSS hooks"` -> selected test passed.
- GREEN: `npm test -- src/renderer/materialDesign.test.ts src/renderer/chatOutputModel.test.ts src/renderer/chatMarkdown.test.ts src/renderer/chatStreamReducer.test.ts src/shared/chatOutput.test.ts` -> 5 files / 82 tests passed.
- Verification: `npm run build` passed with the existing Vite chunk-size warning; `npm run harness:check` passed; `npm run smoke:prod` passed with the existing better-sqlite3 ABI JSON fallback; `git diff --check` passed.
- Browser QA rerun: desktop 1280x900 had body-only one-column output, evidence-linked two-column output, full-width title-only ledger, no page overflow, no overflowing cards, and no console warn/error logs.
- Browser QA rerun: mobile 390x844 had body-only and evidence-linked answer blocks both as one 325px column, title-only ledger as one 303px column, no page overflow, no overflowing cards, one expected internally scrolling table, and no console warn/error logs.

## Implementation Notes

- `AnswerBlock` now exposes `.chat-answer-body` while retaining the body/evidence split.
- Code diff rendering now emits `.chat-diff-line-added`, `.chat-diff-line-removed`, and context spans for styled patch previews.
- Artifact, citation, approval, guided input, and run ledger renderers expose stable hooks required for visual polish.
- `chat.css` adds compact answer-led output styling, evidence rails, table/code/terminal/json surfaces, approval/input/diagnostic states, and ledger rows.
- `responsive.css` stacks `.chat-answer-block` at narrow widths and keeps tables, code, terminal, and JSON previews horizontally safe.
- Added the static repo-local artifact covering evidence-linked answer, run ledger, table, code/diff, terminal output, document report, approval waiting, guided input, error diagnostic, and narrow layout.
