# Build Resources

Electron Builder reads macOS packaging resources from this directory.

Current assets:

- `icon.svg`: editable source icon.
- `icon.icns`: macOS application icon used by Electron Builder.

Future production assets such as signing entitlements and notarization helpers should live here.
